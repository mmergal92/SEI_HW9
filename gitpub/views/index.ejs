<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href= "/public/assets/style.css" rel="stylesheet" type="text/css" />
    <title>Drinks Index</title>
</head>
<body>
    <h1>Welcome to GitPub</h1>
    <!-- <style type="text/css">
        body {
          color: blanchedalmond;
          background-color: steelblue;
        }
      </style> -->
    <h2>Full Menu</h2>  
    <ul>
        <% for (let i = 0; i < allDrinks.length; i++) { %>
        <li>
          <a href="/drinks/<%=i%>">
            <%=allDrinks[i].name.charAt(0).toUpperCase()+ allDrinks[i].name.substring(1)%>
          </a>
        </li>
        <% } %>
    </ul>
    <h3>Food Menu</h3> 
    <ul>
        <% for (let i = 0; i < allFood.length; i++) { %>
        <li>
          <a href="/food/<%=i%>">
            <%=allFood[i].name.charAt(0).toUpperCase()+ allFood[i].name.substring(1)%>
          </a>
        </li>
        <% } %>
    </ul>

</body>
</html>